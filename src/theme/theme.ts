import { createTheme } from "@mui/material";

export const theme = createTheme({
	typography: {
		fontFamily: 'Noto san JP, Roboto, sans-serif',
		fontWeightRegular: 400,
		fontWeightMedium: 500,
		fontWeightBold: 700
	}
});